# Band catalogue

One YAML file per brand, validated by `../schema/bands.schema.json`. The global
catalogue is empty until real manufacturer data is researched — no band
figures are invented here. See `docs/CONTENT_AUTHORING.md`.
