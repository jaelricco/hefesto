# Development image: hot reload via air, source bind-mounted from the host.
FROM golang:1.24-alpine
WORKDIR /app
RUN apk add --no-cache git ca-certificates curl && \
    go install github.com/air-verse/air@latest
ENV CGO_ENABLED=0
EXPOSE 8080
CMD ["air", "-c", ".air.toml"]
