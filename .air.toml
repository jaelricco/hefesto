root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  include_ext = ["go", "yaml", "sql"]
  exclude_dir = ["tmp", "ios", "content", "docs", ".git"]
  delay = 300
  stop_on_error = true

[log]
  time = true
